import { styled } from "@mui/material";

/**
 * Represents a styled label component.
 */
const label = styled("label")`
  padding: 0 0 4px;
  line-height: 1.5;
  display: block;
`;
export default label;
